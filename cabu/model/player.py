from card import Card

from typing import List

class Player:
    def __init__(self, name: String, score: String, card: list[Card]):
        self.name = name
        self.score = score
        self.card = card

    
    
